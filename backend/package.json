{
  "name": "collab-task-backend",
  "version": "1.0.0",
  "description": "Backend API for Collaborative Task Management app",
  "main": "src/server.js",
  "scripts": {
    "build": "npx prisma generate",
    "start": "npx prisma db push && node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.2.0",
    "prisma": "^5.10.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
